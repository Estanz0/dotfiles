# Overrides for Plugins
alias cat='bat'
alias vi='nvim'
alias cd='z'

alias ls='lsd'


# Frequently used commands
alias type='type -a'
alias md='mkdir -p'
alias agr='alias | grep'
alias cls='clear'
alias cx='chmod +x'

alias l='ls -l'
alias la='ls -a'
alias lsa='ls -la'
alias lt='ls --tree'

# Pip
alias pip='noglob pip'